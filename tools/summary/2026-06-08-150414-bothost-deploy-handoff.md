# Handoff Summary - BotHost Deploy Prep

Date: 2026-06-08

## Current State

- Working tree is clean after the latest push.
- Current branch: `main`.
- Remote: `origin` -> `https://github.com/Dimosfil/aromatsmysla.git`.
- Latest pushed commits:
  - `3ba2c6d Fix BotHost Docker build storage sources`
  - `48c5aa1 Prepare BotHost deployment package`
- Local bot runtime was stopped after a brief start attempt. Port `3000` was
  verified free after stopping the Node processes.

## What Changed

- Added a deployable BotHost package around the standalone Node.js Telegram bot.
- Root `Dockerfile` builds from `standalone/` and runs the compiled API:
  `node apps/api/dist/main.js`.
- Added BotHost env examples and deployment docs:
  - `env.bothost.example`
  - `DEPLOY_REPO.md`
  - `README.md`
- Added bot seed content and guide PDFs under both profile-level folders and
  `standalone/bot/`.
- Fixed UTF-8 seed content; Python verification showed `Привет! 🌿`.
- Fixed `.gitignore` / `.dockerignore` so runtime `storage/` remains ignored,
  but source directories are included:
  - `standalone/apps/api/src/storage/`
  - `standalone/packages/core/src/storage/`

## BotHost Notes

- BotHost must use `Node.js`, not Python.
- Enable `Use custom Dockerfile`.
- Leave `Main file` empty.
- Use:
  - Git URL: `https://github.com/Dimosfil/aromatsmysla.git`
  - Branch: `main`
  - Location: Netherlands
- If BotHost tries to run `standalone/packages/core/src/ports.ts`, it is using
  autogenerated Dockerfile settings instead of the repository Dockerfile.
- A previous Docker build failed because `src/storage` files were ignored and
  missing from Git. This was fixed and pushed in `3ba2c6d`.

## Verification Already Run

From `standalone/`:

- `npm run build` passed.
- `npm run test` passed.
- `npm run smoke:api` passed.

Docker build was not verified locally because Docker Desktop daemon was not
running when checked earlier.

## Safety Notes

- `.env.local`, logs, runtime `data/`, `node_modules`, and `dist` are ignored.
- Secret scan found no real token/key in committed files; only a fake test token
  in `standalone/apps/api/src/tests/telegramGateway.test.ts`.

