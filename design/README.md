# Design

Shared design tokens for web surfaces live here.

- `tokens.css`: spacing, radius, and base color variables.
- `themes.css`: theme-level color assignments.

Applications should import these files instead of redefining base visual values.
